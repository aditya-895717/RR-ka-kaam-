django>=4.2,<5.0
djangorestframework>=3.14
django-allauth[socialaccount]>=0.54
django-cors-headers>=4.0
python-decouple>=3.8
sib-api-v3-sdk>=7.6
whitenoise>=6.6
Jinja2>=3.1

# Neon (managed Postgres) — DATABASE_URL parsing + driver
dj-database-url>=2.1
psycopg2-binary>=2.9

# Not used by Vercel (which supplies its own WSGI handler), but kept so the
# surviving Procfile `web:` line stays runnable for local production-parity
# testing. Safe to drop if the Procfile is deleted.
gunicorn>=21.0

# REMOVED: django-q2 — requires a long-lived `manage.py qcluster` process, which
# Vercel's serverless Python runtime cannot provide. Replaced by the
# token-protected /api/notifications/sweep/ endpoint driven by an external pinger.
